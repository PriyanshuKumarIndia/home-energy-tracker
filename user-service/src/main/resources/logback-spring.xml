<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <!-- =========================================================
         Properties
         ========================================================= -->

    <property name="LOG_DIR" value="${LOG_DIR:-logs}"/>
    <property name="APP_NAME" value="${spring.application.name:-user-service}"/>

    <!-- =========================================================
         Console Appender
         ========================================================= -->

    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level ${APP_NAME} [%X{traceId:-}] %logger{36} - %msg%n
            </pattern>
        </encoder>

    </appender>


    <!-- =========================================================
         Rolling Application Log
         ========================================================= -->

    <appender name="APPLICATION_FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_DIR}/${APP_NAME}.log</file>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level ${APP_NAME} [%X{traceId:-}] %logger{36} - %msg%n
            </pattern>
        </encoder>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <fileNamePattern>
                ${LOG_DIR}/archive/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz
            </fileNamePattern>

            <!-- New file every day -->
            <maxFileSize>50MB</maxFileSize>

            <!-- Keep logs for 30 days -->
            <maxHistory>30</maxHistory>

            <!-- Maximum total size of archived logs -->
            <totalSizeCap>2GB</totalSizeCap>

        </rollingPolicy>

    </appender>


    <!-- =========================================================
         Error Log
         ========================================================= -->

    <appender name="ERROR_FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_DIR}/${APP_NAME}-error.log</file>

        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level ${APP_NAME} [%X{traceId:-}] %logger{36} - %msg%n
            </pattern>
        </encoder>

        <rollingPolicy
                class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">

            <fileNamePattern>
                ${LOG_DIR}/archive/${APP_NAME}-error.%d{yyyy-MM-dd}.%i.log.gz
            </fileNamePattern>

            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>

        </rollingPolicy>

    </appender>


    <!-- =========================================================
         Application Logging
         ========================================================= -->

    <logger name="com.teamengineoil.user_service"
            level="DEBUG"/>


    <!-- =========================================================
         Spring Framework
         ========================================================= -->

    <logger name="org.springframework"
            level="INFO"/>


    <!-- =========================================================
         Hibernate
         ========================================================= -->

    <logger name="org.hibernate"
            level="WARN"/>

    <logger name="org.hibernate.SQL"
            level="WARN"/>


    <!-- =========================================================
         Root Logger
         ========================================================= -->

    <root level="INFO">

        <appender-ref ref="CONSOLE"/>

        <appender-ref ref="APPLICATION_FILE"/>

        <appender-ref ref="ERROR_FILE"/>

    </root>

</configuration>